#include<stdio.h>
#include<math.h>

int dork(int n)
{  
  int i;
  int x;
  int treedepth=1;
  for(i=1;i<=n;i++)
  {
    
    q=fork();
    if(q!=0)return(treedepth);
    else
    {
     return(treedepth+1) 
 
